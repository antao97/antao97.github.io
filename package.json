{
  "name": "personal-homepage",
  "version": "1.0.0",
  "description": "Personal homepage with Chinese and English versions",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "nodemon --watch data --ext json scripts/build.js"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}